# Utilities

## ArchiSteamFarm

ASF is used for Steam automation and card farming.

### Exposed Ports
- 1242/tcp – ASF IPC / web interface

### Volumes
- `/app/config` – ASF configuration (contains credentials)
- `/app/plugins` – ASF plugins
- `/app/logs` – Logs
